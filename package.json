{
  "name": "remote-photo-slides",
  "version": "1.0.0",
  "private": true,
  "description": "Home Wi-Fi photo folder slideshow server",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/admin.js && node --check public/slideshow.js"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
